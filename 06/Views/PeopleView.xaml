<UserControl x:Class="Mach_rocnikova_prace.Views.PeopleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mach_rocnikova_prace.Views"
             mc:Ignorable="d" 
             Background="White"
             
             xmlns:controls="clr-namespace:Mach_rocnikova_prace.Controls"
             
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Margin="10">
        

        <!--Zobrazení výsledku hledání-->
        <DataGrid ItemsSource="{Binding People}" AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Příjmení" Binding="{Binding LastName}"/>
                <DataGridTextColumn Header="Jméno" Binding="{Binding FirstName}"/>
                <DataGridTextColumn Header="Datum narození" Binding="{Binding DateOfBirth, StringFormat=dd.MM.yyyy}"/>
                <DataGridTextColumn Header="Rodné číslo" Binding="{Binding BirthNumber}"/>
                <DataGridTextColumn Header="Zdravotní pojišťovna" Binding="{Binding InsuranceCompanyName}"/>
                <DataGridTextColumn Header="Město" Binding="{Binding City}"/>
                <DataGridTextColumn Header="Ulice" Binding="{Binding StreetName}"/>
                <DataGridTextColumn Header="Číslo popisné" Binding="{Binding PostalNumber}"/>
                <DataGridTextColumn Header="PSČ" Binding="{Binding PostalCode}"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role.Name}"/>
                <DataGridTextColumn Header="Tým" Binding="{Binding Team.Name}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
