<Window x:Class="Mach_rocnikova_prace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mach_rocnikova_prace"
        
        xmlns:controls="clr-namespace:Mach_rocnikova_prace.Controls"
        
        mc:Ignorable="d" Background="White"
        Title="Rocnikovka" Height="450" Width="800">
    <Grid>
        <!--Rozdělení na dvě části hlavička + tělo-->
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>
        
        <!--Rozdělení hlavičky-->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="80*"/>
            </Grid.ColumnDefinitions>

            <controls:CompanyIdentity/>

        </Grid>
        
        <!--Rozdělení těla-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="80*"/>
            </Grid.ColumnDefinitions>
            
            <!--Navigation bar-->
            <controls:NavigationBar Grid.Column="0" DataContext="{Binding Navigator}"/>
            <ContentControl Grid.Column="1" Content="{Binding Navigator.CurrentViewModel}"/>

        </Grid>
    </Grid>
</Window>
