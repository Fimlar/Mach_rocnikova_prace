<UserControl x:Class="Mach_rocnikova_prace.Views.PeopleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mach_rocnikova_prace.Views"
             mc:Ignorable="d" 
             Background="White"
             
             xmlns:controls="clr-namespace:Mach_rocnikova_prace.Controls"
             
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>
        <!--Panel hledání-->
        <controls:SearchPanel VerticalAlignment="Bottom" Margin="0,5"/>

        <!--Zobrazení výsledku hledání-->
        <DataGrid Grid.Row="1" ItemsSource="{Binding People}" AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Příjmení" Binding="{Binding LastName}"/>
                <DataGridTextColumn Header="Jméno" Binding="{Binding FirstName}"/>
                <DataGridTextColumn Header="Zdravotní pojišťovna" Binding="{Binding InsuranceCompanyName}"/>
                <DataGridTextColumn Header="Rodné číslo" Binding="{Binding BirthNumber}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
